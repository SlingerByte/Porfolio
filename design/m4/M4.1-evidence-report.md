# M4.1 — Content Intake & Evidence Collection

Fecha: 2026-08-25 · Investigación local de los 4 repositorios. Sin cambios en código de producción.

Leyenda: **VERIFIED** = comprobado en disco · **USER-PROVIDED** = afirmación del usuario · **INFERRED** = interpretación razonable del código · **UNKNOWN** = no publicar hasta confirmar.

Repositorios inspeccionados:

| Proyecto | Ruta local | Git |
|---|---|---|
| GrantFlow | `Escritorio\GrantFlow` | ✅ 7 commits (`SlingerByte`) |
| EcoFunding | `~\EcoFunding.git` (bare) | ✅ 68 commits (`Emilson Oviedo`: 65) |
| VoxLab | `Escritorio\fun-cosy` | ✅ 3 commits (`SlingerByte`) |
| Blip | `Escritorio\Blip` | ❌ sin `.git` |

---

## 1. GrantFlow

```text
Project:   GrantFlow
Type:      Open-source, local-first AI system (web app + API)
Status:    VERIFIED "MVP in development" (README)

Problem:   Fundaciones/nonprofits no logran descubrir oportunidades de
           financiación relevantes (VERIFIED README + USER-PROVIDED contexto)
Target:    Organizaciones sin ánimo de lucro (VERIFIED vision/product docs)
Solution:  Dado un perfil organizacional: descubre, evalúa elegibilidad,
           verifica claims contra evidencia y prioriza oportunidades con
           fuentes trazables. Dos modos: determinista (retrieval→ranking sin
           LLM) y LLM-assisted (agéntico con trace estructurado).

My role:   USER-PROVIDED (autor). Git local: 7 commits por `SlingerByte`
           (historial reciente/re-baseline — UNKNOWN si existe historial previo).
Verified responsibilities: autoría del repo completo (código+docs+tests+evals).

Frontend:       React 18 + TypeScript + Vite (VERIFIED package.json; deps
                mínimas: solo fuentes — UI propia sin librería de componentes)
Backend:        Python + FastAPI + SQLAlchemy 2 async + Alembic (VERIFIED)
Database:       PostgreSQL + pgvector vía Docker Compose (VERIFIED)
AI/ML:          LLM local + embeddings vía Ollama, provider-agnostic (VERIFIED)
Cloud:          Ninguno por diseño — ADR 0007 "no-auth cloud MVP" (VERIFIED)
Infrastructure: Docker Compose (PG/pgvector + SearXNG gestionado por el
                proyecto), pnpm workspace root (VERIFIED)
Other:          SearXNG para web-discovery con gating determinista (VERIFIED)

Architecture:   Módulos backend verificados: api / assessment / cli / db /
                embeddings / ingestion / judgment / llm / research / retrieval /
                schemas / services / workflow.
                **21 ADRs documentadas** (monorepo, provenance, pgvector,
                ranking determinista, provider abstraction, SSRF hardening,
                hybrid retrieval, web discovery, human review…), threat model
                de seguridad y doc de workflow agéntico. (VERIFIED — inusual
                y muy valioso a nivel junior)
Challenges:     Degradación elegante cuando el LLM local cae; descubrimiento
                web auditable ("nunca silenciosamente swallow"); ingesta
                offline; provenance de evidencia. (VERIFIED via ADRs/docs)

Tests:        ✅ 60 archivos test_*.py + gate `pnpm verify` fully-offline
Performance:  framework de evaluación propio: benchmark/replay/judgment/
              live runners + datasets + results persistidos (VERIFIED files;
              métricas concretas UNKNOWN hasta revisar results/)
Metrics:      UNKNOWN (requiere extraer números de evaluation/results)

Repository:   local; UNKNOWN URL pública / visibilidad
Demo:         UNKNOWN
Evidence:     README + 21 ADRs + threat model + 60 tests + evals framework +
              docker-compose reproducible
```

**Fortalezas:** el proyecto estrella técnico — AI engineering real (RAG/hybrid retrieval/judgment/agentic) con disciplina de ingeniería rara a nivel junior (ADRs, threat model, offline verification gate). Encaja perfecto con el posicionamiento AI. Local-first/private = historia coherente.
**Debilidades:** MVP en desarrollo; historial git corto localmente; sin demo pública confirmada; frontend deliberadamente minimal.
**Faltante:** URL pública, screenshots, métricas de evaluación extraídas.

---

## 2. EcoFunding

```text
Project:   EcoFunding
Type:      Professional project (Microsoft experience) — automated grant
           discovery & evaluation pipeline for a specific NGO
Status:    VERIFIED desarrollo histórico completo (bare repo, 68 commits);
           estado actual del despliegue UNKNOWN

Problem:   Reducir el tiempo de búsqueda de financiación relevante
           (VERIFIED README)
Target:    Instituto de Conservación de Ballenas (ICB), Argentina
           (VERIFIED README + link icb.org.ar)
Solution:  Pipeline de dos etapas desacopladas:
           Etapa 1 "Explorador": genera queries, rastrea la web, filtro
           semántico, cola de URLs pending.
           Etapa 2 "Cazador": scraping (curl_cffi + BeautifulSoup) + evaluación
           contra criterios éticos/científicos/geográficos del ICB sobre el
           mismo registro (sin duplicados).

My role:   USER-PROVIDED (desarrollado durante experiencia Microsoft).
           VERIFIED: 65/68 commits por `Emilson Oviedo`; 3 colaboradores
           ocasionales (SlingerByte, Wilmer, Juan Sebastián Cárdenas-Rodríguez).

Frontend:       React 18 (VERIFIED badges/tree)
Backend:        Python 3.11 + FastAPI (VERIFIED)
Database:       PostgreSQL 16 (VERIFIED)
AI/ML:          SentenceTransformer para filtro semántico (VERIFIED commit
                "Load SentenceTransformer model in Dockerfile" + evaluator)
Cloud:          Azure badge en README (VERIFIED badge; detalles de deploy UNKNOWN)
Infrastructure: Docker + docker-compose + CI GitHub Actions (VERIFIED tree/
                commit "disk space cleanup step in CI")
Other:          curl_cffi, BeautifulSoup, PDM

Architecture:   monorepo backend/frontend/tests, dos etapas desacopladas
                via cola en DB (VERIFIED README)
Challenges:     anti-bot scraping (curl_cffi), evaluación semántica de
                criterios de una ONG real, deduplicación (INFERRED/VERIFIED mix)

Tests:        ✅ carpeta tests/ existe (VERIFIED); cobertura UNKNOWN
Metrics:      UNKNOWN (ahorro de tiempo afirmado por usuario, no cuantificado)
Repository:   local bare; visibilidad pública UNKNOWN
Demo:         UNKNOWN
Evidence:     README completo en español con arquitectura detallada + 65
              commits propios + CI
```

**Fortalezas:** ÚNICO proyecto con cliente real (ONG) y contexto profesional Microsoft — credenciales irrepetibles para junior. Historia narrativa potente (ballenas → Patagonia → impacto). Relación directa con GrantFlow = muestra evolución profesional.
**Debilidades:** riesgo de confusión con GrantFlow (mitigable narrándolos como secuela); visibilidad pública probablemente limitada por NDA/contexto — **UNKNOWN crítico**.
**Faltante:** permiso para nombrar Microsoft/ICB públicamente, screenshots, métricas de tiempo ahorrado.

---

## 3. VoxLab

```text
Project:   VoxLab (repo local: fun-cosy)
Type:      Laboratorio local de voice-cloning/TTS + benchmarking (web app)
Status:    VERIFIED activo (baselines M2c/M3c en git)

Problem:   Experimentar con clonación de voz local sin servicios cloud
           (VERIFIED README: "no cloud APIs; recordings never leave your disk")
Target:    El propio desarrollador / comunidad de experimentación (INFERRED)
Solution:  Web UI local sobre Fun-CosyVoice3-0.5B-2512: sube referencia →
           transcripción ASR local editable → emociones/intensidad/speed/
           volume → audio clonado en el navegador. Model-agnostic por diseño.

My role:   USER-PROVIDED (autor). Git: 3 commits `SlingerByte` (baselines
           recientes; UNKNOWN historial previo pre-baseline).

Frontend:       ✅ existe carpeta frontend + pnpm dev dual-service (VERIFIED;
                stack detallado UNKNOWN hasta leer su package.json)
Backend:        Python FastAPI server (VERIFIED: main/config/schemas/audio/
                expressions/health/instrumentation/predownload_asr)
Database:       No aplica (estado local) (INFERRED)
AI/ML:          Fun-CosyVoice3-0.5B TTS + ASR local (VERIFIED README);
                experimentación previa con F5-TTS detectada en C:\dev\F5-TTS
                (VERIFIED existencia; relación con VoxLab INFERRED)
Cloud:          Ninguno — todo local incl. inferencia GPU (VERIFIED README)
Infrastructure: start/validate scripts multiplataforma, requirements.txt
Other:          Benchmarking framework propio con persistencia de
                experimentos + reports JSON (benchmark/{runner,experiments,
                results.json,report.json}) (VERIFIED)

Architecture:   CLI lab (Milestone-0, examples intactos) + capa server + web UI
Challenges:     serving de modelos TTS pesados localmente, instrumentación/
                telemetría de inferencia, benchmarking reproducible

Tests:        ✅ 12 archivos test_*.py (VERIFIED)
Performance:  benchmark runner + reports persistidos (VERIFIED estructura;
              números concretos UNKNOWN hasta leer report.json)
Metrics:      UNKNOWN (extraíbles de benchmark/*/report.json)
Repository:   local; UNKNOWN público
Demo:         local por diseño; grabaciones posibles (samples/, outputs/,
              reference_synthetic_440hz.wav en backups) (VERIFIED files)
Evidence:     README + tests + benchmarks + samples de audio
```

**Fortalezas:** demuestra AI aplicada de nicho (voz/GPU/benchmarking) que ningún otro candidato cubre; historia "todo local, nada sale de tu disco" muy contable; telemetría/benchmarking = madurez de ingeniería.
**Debilidades:** nicho; git joven; frontend poco auditado aún.
**Faltante:** URL pública, demo en vídeo/audio para el portfolio, detalle del frontend.

---

## 4. Blip

```text
Project:   Blip
Type:      Plataforma de delivery multi-audiencia (proyecto final SENA)
Status:    VERIFIED monorepo activo en disco; SIN .git (sin historial)

Problem:   Delivery más justa con repartidores y vehículos ecológicos
           (USER-PROVIDED; PRODUCT.md confirma bicicleta/scooter eléctrico
           como audiencia — VERIFIED parcial)
Target:    4 audiencias VERIFIED en PRODUCT.md: cliente final (Colombia,
           es-CO/COP), comercio local, domiciliario, admin plataforma
Solution:  Marketplace de delivery: pedidos, catálogos/promociones, panel
           comercio, asignación por enlace WhatsApp, mapa y tracking,
           dashboard admin con métricas

My role:   USER-PROVIDED (proyecto final propio). SIN git → responsabilidades
           no verificables por historial.

Frontend:       VERIFIED React 19 + Vite 7 + TS + Tailwind 4 + shadcn/ui
                (Radix) + TanStack Query + react-hook-form + MapLibre-gl +
                Protomaps/pmtiles + Nominatim (geocoding OSM) + Framer Motion
                + recharts + jspdf + dnd-kit + tsparticles
Backend:        VERIFIED AdonisJS 6 + Lucid + Vine + auth + bouncer
                (policies/abilities) + socket.io + node-cron + drive
Database:       PostgreSQL via pg; Supabase mencionado en README (VERIFIED
                mención; configuración efectiva UNKNOWN)
AI/ML:          Ninguno (VERIFIED ausencia)
Cloud:          Cloudinary para uploads (VERIFIED dep backend + hook frontend
                useCloudinaryUpload + uso en ProductForm)
Infrastructure: pnpm workspaces monorepo (VERIFIED)
Other:          ⚠️ Stack histórico user-provided decía "Google Maps + Leaflet":
                **NO PRESENTE** en el código actual (VERIFIED grep) — hoy es
                MapLibre + Protomaps + Nominatim. Publicar el stack ACTUAL.

Architecture:   apps/frontend + apps/backend, path aliases, orquestación root
Challenges:     multi-rol con auth/policies, realtime (socket.io), mapas,
                pagos?? (UNKNOWN — no verificado pago real)

Tests:        ⚠️ setup Japa presente pero tests/ contiene solo bootstrap.ts
              → 0 specs encontrados (VERIFIED ausencia)
Metrics:      UNKNOWN
Repository:   local sin git; UNKNOWN público
Demo:         UNKNOWN (existe dist/ compilado → hubo build funcional, INFERRED)
Evidence:     PRODUCT.md detallado (audiencias, propósito) + código fuente
```

**Fortalezas:** el más "producto completo" (4 audiencias, realtime, mapas, panel admin) — demuestra amplitud full-stack y capacidad de construir software de negocio.
**Debilidades:** sin git (evidencia de proceso nula), sin tests, sin AI (no aporta al posicionamiento AI), stack real difiere del recordado.
**Faltante:** screenshots, demo, init de git o export de historial si existió, aclarar pagos.

---

## 5. Matriz comparativa rápida

| | GrantFlow | EcoFunding | VoxLab | Blip |
|---|---|---|---|---|
| Evidencia técnica | ★★★ (ADRs+tests+evals) | ★★☆ (git 65 commits+CI) | ★★☆ (tests+benchmarks) | ★☆☆ (sin git/sin tests) |
| Relevancia AI | ★★★ | ★★☆ (semantic ML) | ★★★ (voice/GPU) | ☆ |
| Variedad vs otros | retrieval/agentic | scraping/evaluación ONG | voz/GPU local | full-stack producto |
| Cliente/pro real | ✗ (open-source) | ✓ ICB/Microsoft | ✗ | semi (SENA) |
| Clasificación tentativa | **PRIMARY** | **PRIMARY** | **PRIMARY** | SECONDARY |

Variedad técnica entre los 3 PRIMARY: retrieval/agéntico + scraping/evaluación semántica + voz/GPU local → cobertura AI amplia sin redundancia. Blip aporta amplitud full-stack como complemento.

---

## 6. Gaps que requieren input del usuario

1. Visibilidad pública (URLs) de cada repo/demo — bloqueante para cards.
2. Permiso para citar Microsoft e ICB públicamente (EcoFunding).
3. Métricas: extraer `evaluation/results` (GF) y `benchmark/report.json` (VoxLab) — puedo hacerlo en siguiente pasada; y cifras de impacto de EcoFunding si existen.
4. Screenshots por proyecto (ninguno existe en los repos inspeccionados salvo assets/logo de EcoFunding).
5. Blip: ¿inicializar git? ¿existía historial? ¿hubo integración de pagos?
6. Rol exacto del usuario en cada uno (confirmar autoría única GF/VoxLab).
